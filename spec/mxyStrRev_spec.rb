require 'spec_helper'

describe MxyStrRev do
  it 'has a version number' do
    expect(MxyStrRev::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
